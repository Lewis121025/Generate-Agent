# Lewis AI System - 环境变量配置示例
# 复制此文件为 .env 并填写你的配置

# ========================================
# 核心配置
# ========================================
APP_ENV=development
LOG_LEVEL=INFO

# 安全密钥（生产环境请使用随机生成的32字符密钥）
# 使用: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-here-must-change-in-production
API_KEY_SALT=your-api-key-salt-here-must-change-in-production

# ========================================
# 数据库配置
# ========================================
# PostgreSQL数据库连接（推荐配置，否则使用内存存储）
DATABASE_URL=postgresql+asyncpg://lewis:lewis_pass@localhost:5432/lewis_db
# Docker环境: postgresql+asyncpg://lewis:lewis_pass@postgres:5432/lewis_db

# ========================================
# Redis 缓存（推荐）
# ========================================
REDIS_URL=redis://localhost:6379/0
REDIS_ENABLED=true
# Docker环境: redis://redis:6379/0

# ========================================
# 向量数据库（可选）
# ========================================
VECTOR_DB_TYPE=weaviate
VECTOR_DB_URL=http://localhost:8080
# Docker环境: http://weaviate:8080

# ========================================
# AI Provider API Keys
# ========================================

# LLM Provider配置
LLM_PROVIDER_MODE=openrouter
# 支持: openrouter, mock

# OpenRouter (必需 - 用于LLM调用)
OPENROUTER_API_KEY=sk-or-v1-your-openrouter-api-key-here

# 视频生成 Providers（至少配置一个）
VIDEO_PROVIDER=runway
# 支持: runway, pika, doubao, runware, mock

RUNWAY_API_KEY=your-runway-api-key-here
PIKA_API_KEY=your-pika-api-key-here
DOUBAO_API_KEY=your-doubao-api-key-here
RUNWARE_API_KEY=your-runware-api-key-here

# 语音合成（可选）
ELEVENLABS_API_KEY=your-elevenlabs-api-key-here

# 网页搜索和抓取（可选）
TAVILY_API_KEY=your-tavily-api-key-here
FIRECRAWL_API_KEY=your-firecrawl-api-key-here

# ========================================
# 对象存储（推荐配置用于生产环境）
# ========================================
# S3或兼容服务配置（MinIO、阿里云OSS、腾讯云COS等）
S3_ACCESS_KEY=your-s3-access-key
S3_SECRET_KEY=your-s3-secret-key
S3_REGION=us-east-1
S3_BUCKET_NAME=lewis-artifacts
S3_ENDPOINT_URL=https://s3.amazonaws.com
# 如果不配置，将使用本地文件存储

# ========================================
# 网络配置
# ========================================
CORS_ORIGINS=http://localhost:3000,http://localhost:8000
TRUSTED_HOSTS=localhost,127.0.0.1
# 生产环境请配置具体的域名

# 代理配置（可选）
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080

# ========================================
# 前端配置
# ========================================
NEXT_PUBLIC_API_BASE=http://localhost:8000
# 生产环境: https://api.yourdomain.com

# ========================================
# 安全和限流
# ========================================
RATE_LIMIT_ENABLED=true
RATE_LIMIT_PER_MINUTE=60

# ========================================
# 预算和限制
# ========================================
DEFAULT_BUDGET_LIMIT_USD=50.0
MAX_ITERATIONS_PER_SESSION=10
MAX_VIDEO_DURATION_SECONDS=300

# ========================================
# 沙箱配置
# ========================================
SANDBOX_TIMEOUT_SECONDS=30
SANDBOX_MAX_OUTPUT_SIZE=1048576
